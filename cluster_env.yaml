# This file is used to build the cluster environment for a Service with tracing.
#
# Anyscale command:
# anyscale cluster-env build cluster_env.yaml --name tracing-cluster-env

docker_image: my-registry/my-image:tag  # <--- replace with your image
ray_version: 2.9.0  # <--- replace if your image is based off different Ray version